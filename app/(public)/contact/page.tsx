'use client'; import { useState } from 'react';
export default function Contact(){const [state,setState]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('');const r=await fetch('/api/enquiries',{method:'POST',body:new FormData(e.currentTarget)});if(r.ok){e.currentTarget.reset();setState('Thank you. Your requirement has been received.')}else setState('We could not submit your requirement. Please try again.')}return <main className="shell page"><h1>Send a Requirement</h1><p className="lede">Tell us what you are looking for. Fields marked below are required.</p><form className="form" onSubmit={submit}><input name="name" required placeholder="Name"/><input name="email" required type="email" placeholder="Email"/><input name="phone" required placeholder="Phone / WhatsApp"/><textarea name="requirement" required placeholder="What are you looking for?"/><input name="website" tabIndex={-1} autoComplete="off" style={{display:'none'}}/><button className="button">Send requirement</button>{state&&<p className="notice">{state}</p>}</form></main>}
