import { notFound,redirect } from 'next/navigation'; import { hasSession } from '@/lib/server/auth'; import { getCategoryById } from '@/lib/server/categories'; import CategoryForm from '../../category-form'; import { updateCategoryAction } from '../../actions';
export default async function EditCategoryPage({params}:{params:Promise<{id:string}>}){if(!await hasSession())redirect('/admin/login');const{id}=await params;const category=await getCategoryById(id);if(!category)notFound();return <main className="admin admin-products"><a href="/admin/categories">← Categories</a><h1>Edit Category</h1><CategoryForm action={updateCategoryAction.bind(null,id)} category={category}/></main>}
