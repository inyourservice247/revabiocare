import Link from 'next/link';
import { services } from '@/data/demo-content';
import { getFeaturedActiveProducts } from '@/lib/server/products';

export const dynamic = 'force-dynamic';

const snapshot = ['Product-led enquiries', 'Documentation coordination', 'Commercial responsiveness', 'Requirement-based sourcing'];

export default async function Home() {
  const products = await getFeaturedActiveProducts();
  return <main><section className="hero"><div className="shell"><p className="eyebrow">PHARMACEUTICAL BUSINESS</p><h1>A clearer path from requirement to commercial discussion.</h1><p className="lede">A focused B2B interface for pharmaceutical procurement, product enquiries, documentation discussions and commercial coordination.</p><div className="hero-actions"><Link className="button" href="/contact">Send a Requirement</Link><Link className="button button-secondary" href="/products">Explore Products</Link></div></div></section><section className="section"><div className="shell snapshot">{snapshot.map(x => <span key={x}>{x}</span>)}</div></section><section className="section section-ivory"><div className="shell"><div className="section-heading"><h2>Explore the product catalogue</h2><p className="lede">Browse the catalogue and start a conversation about your requirement.</p></div>{products.length > 0 ? <div className="grid">{products.map(x => <article className="card" key={x.id}><p className="eyebrow">{x.category}</p><h3><Link className="product-name-link" href={`/products/${x.slug}`}>{x.name}</Link></h3><p>{x.overview}</p><Link href={'/contact?requirement=' + encodeURIComponent(x.name)}>Send Requirement →</Link></article>)}</div> : <p className="lede">No featured products are selected at this time.</p>}<p><Link className="button" href="/products">View All Products</Link></p></div></section><section className="section"><div className="shell"><div className="section-heading"><h2>Built around the requirement</h2><p className="lede">Commercial service areas supporting requirement-led discussions.</p></div><div className="service-list">{services.slice(0, 4).map(x => <article key={x}><h3>{x}</h3><p>Commercial support for {x.toLowerCase()} within a pharmaceutical business discussion.</p></article>)}</div><p><Link className="button" href="/services">Explore Services</Link></p></div></section><section className="section section-ivory"><div className="shell"><h2>Documentation belongs in the commercial conversation.</h2><p className="lede">Documentation and compliance information is discussed in relation to the relevant product, source or business arrangement.</p><div className="tag-list">{['COA', 'Specifications', 'SDS', 'Technical Documentation', 'Regulatory Documentation Requests'].map(x => <span key={x}>{x}</span>)}</div><p>Availability varies by product and source.</p><Link className="button" href="/quality-compliance">Quality & Compliance</Link></div></section><section className="section"><div className="shell"><h2>How a requirement moves forward</h2><div className="process">{[['01', 'Share Requirement', 'Tell us the product, quantity or commercial requirement.'], ['02', 'Requirement Review', 'The enquiry is reviewed against the relevant product and sourcing context.'], ['03', 'Documentation & Commercial Discussion', 'Applicable specifications, documentation and commercial points can be discussed.'], ['04', 'Follow-up', 'The conversation proceeds according to the specific requirement.']].map(([n, h, p]) => <article key={n}><b>{n}</b><h3>{h}</h3><p>{p}</p></article>)}</div></div></section><section className="final-cta"><div className="shell"><h2>Have a requirement to discuss?</h2><Link className="button" href="/contact">Send a Requirement</Link></div></section></main>;
}
