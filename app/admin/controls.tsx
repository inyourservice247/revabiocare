'use client';export default function Controls(){async function push(){if(!('serviceWorker'in navigator)||!process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY)return;const r=await navigator.serviceWorker.register('/sw.js'),s=await r.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY});await fetch('/api/admin/push-subscriptions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(s)});alert('Notifications enabled.')}return <p><button className="button" onClick={push}>Enable notifications</button></p>}
