'use client';import{useState}from'react';export default function Toggle({id,initial}:{id:string,initial:boolean}){const[v,setV]=useState(initial);async function go(){await fetch('/api/admin/enquiries/'+id,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({isRead:!v})});setV(!v)}return <button className="button" onClick={go}>Mark {v?'unread':'read'}</button>}
