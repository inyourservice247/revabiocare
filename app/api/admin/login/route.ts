import {NextRequest,NextResponse}from'next/server';import{createSession,safeEqual}from'@/lib/server/auth';import{limited}from'@/lib/server/rate-limit';export async function POST(req:NextRequest){const f=await req.formData(),u=String(f.get('username')||''),p=String(f.get('password')||''),ip=req.headers.get('x-forwarded-for')?.split(',')[0]||'unknown';if(await limited('login',ip,8))return NextResponse.json({}, {status:429});if(!process.env.ADMIN_USERNAME||!process.env.ADMIN_PASSWORD||!safeEqual(u,process.env.ADMIN_USERNAME)||!safeEqual(p,process.env.ADMIN_PASSWORD))return NextResponse.json({}, {status:401});await createSession();return NextResponse.json({ok:true});}
