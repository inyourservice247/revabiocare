'use client';import{useState}from'react';export default function Login(){const[e,setE]=useState('');async function go(x:React.FormEvent<HTMLFormElement>){x.preventDefault();const r=await fetch('/api/admin/login',{method:'POST',body:new FormData(x.currentTarget)});if(r.ok)location.href='/admin';else setE('Invalid credentials or too many attempts.')}return <main className="admin"><h1>Admin sign in</h1><form className="form" onSubmit={go}><input name="username" placeholder="Username" required/><input name="password" type="password" placeholder="Password" required/><button className="button">Sign in</button>{e&&<p className="error">{e}</p>}</form></main>}
