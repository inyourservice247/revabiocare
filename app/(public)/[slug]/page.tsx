import { notFound } from 'next/navigation';
const pages:Record<string,{title:string,text:string}>={about:{title:'About Reva Biocare',text:'[PLACEHOLDER — VERIFY BEFORE PRODUCTION] A concise overview will be added once Reva Biocare’s operating model and approved company facts are confirmed.'},products:{title:'Products',text:'[PLACEHOLDER — VERIFY BEFORE PRODUCTION] Product information will be added only when approved for public display.'},services:{title:'Services',text:'[PLACEHOLDER — VERIFY BEFORE PRODUCTION] Service scope will be published after verification.'},'quality-compliance':{title:'Quality & Compliance',text:'[PLACEHOLDER — VERIFY BEFORE PRODUCTION] Request relevant documentation through the enquiry form.'},privacy:{title:'Privacy',text:'We collect the information submitted in an enquiry so Reva Biocare can respond to that business request. Contact mail@revabiocare.com for privacy questions.'}};
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=pages[slug];if(!page)notFound();return <main className="shell page"><h1>{page.title}</h1><p className="lede">{page.text}</p></main>}
